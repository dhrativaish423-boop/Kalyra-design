/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* Agar yahan koi aur configurations ki zaroorat na ho, toh yeh ekdum perfect default config hai */
};

export default nextConfig;
